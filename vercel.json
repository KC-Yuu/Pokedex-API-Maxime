{
  "version": 2,
  "builds": [
    { "src": "config.ru", "use": "@vercel/ruby" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "config.ru" }
  ],
  "build": {
    "env": {
      "BUNDLE_DEPLOYMENT": "false"
    },
    "commands": {
      "build": "bundle config set path 'vendor/bundle' && bundle install && bundle exec rails assets:precompile"
    }
  }
}